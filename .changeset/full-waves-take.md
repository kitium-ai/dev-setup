---
'@kitiumai/dev-setup': major
---

- Added preflight checks (privilege, disk space, network reachability) before running installs.- Improved package manager detection with multiple fallbacks across Windows, macOS, and Linux.- Introduced policy-aware CLI options (allow/block lists), dry-run preview mode, and retry tuning.- Hardened idempotent installs with existing-tool detection and per-task telemetry hooks.- Expanded README with API references and examples for enterprise rollout scenarios.
